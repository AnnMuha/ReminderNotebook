<Window x:Class="ReminderNotebook.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ReminderNotebook.Utils"
        Title="ReminderNotebook"
        Height="500" Width="800"
        Background="#f5f7fa"
        WindowStartupLocation="CenterScreen">

    <!-- Конвертер для кольору пріоритету -->
    <Window.Resources>
        <local:PriorityColorConverter x:Key="PriorityColorConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="📋 Мої нагадування"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="#2c3e50"
                   Margin="0 0 0 20"/>

        <!-- Список нагадувань -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="10"
                Padding="10"
                BorderBrush="#dcdfe3"
                BorderThickness="1">
            <ListBox ItemsSource="{Binding Reminders}"
                     SelectedItem="{Binding SelectedReminder, Mode=TwoWay}"
                     Margin="0"
                     BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" Margin="0 5"
                                CornerRadius="6"
                                Background="#ecf0f1">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}"
                                           FontWeight="Bold"
                                           FontSize="14"/>
                                <TextBlock Text="{Binding ReminderTime, StringFormat=О {0:dd.MM.yyyy HH:mm}}"
                                           Foreground="Gray"
                                           FontSize="12"/>
                                <TextBlock Text="{Binding Priority}"
                                           Foreground="{Binding Priority, Converter={StaticResource PriorityColorConverter}}"
                                           FontSize="12"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0 20 0 0">
            <Button Content="➕ Додати"
                    Width="100"
                    Margin="5"
                    Command="{Binding AddCommand}"
                    Background="#27ae60"
                    Foreground="White"
                    FontWeight="Bold"/>
            <Button Content="✏️ Редагувати"
                    Width="100"
                    Margin="5"
                    Command="{Binding EditCommand}"
                    Background="#2980b9"
                    Foreground="White"
                    FontWeight="Bold"/>
            <Button Content="🗑️ Видалити"
                    Width="100"
                    Margin="5"
                    Command="{Binding DeleteCommand}"
                    Background="#c0392b"
                    Foreground="White"
                    FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>